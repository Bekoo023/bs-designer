# Portfolio screenshots and scope

Captured on 2026-09-16 from https://clauselens.org/ using the browser screenshot API.

- `assets/clauselens-desktop.jpg`: actual public homepage, 1353 × 929 pixels.
- `assets/clauselens-detail.jpg`: 565 × 430 pixel browser screenshot crop of the contract demonstration on that same homepage.
- The user requested these screenshots for their own ClauseLens portfolio case. No customer documents, authenticated screens or private data were used.
- These are public marketing-page screenshots, not evidence of an authenticated analysis workflow. The score and speed claim visible in the original homepage have not been independently measured.
- Design commentary describes observed layout choices, not measured improvements in conversion or user testing results.
- Numico remains based on the previously verified public repository; no new functionality is claimed.
- No mobile product screenshot was captured: the available browser API does not advertise viewport resizing. The portfolio itself includes responsive CSS but its visual verification remains open.

The JPEG files are explicitly allowed by the local preview server and checked byte-for-byte by the static asset tests. This file is not copied into the production build.
